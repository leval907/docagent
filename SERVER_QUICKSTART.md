# 🚀 Быстрое развертывание на сервере (без Docker)

## Предварительная информация
У вас на сервере уже есть:
- PostgreSQL (предполагаю есть)
- Python окружение
- Git

## 📋 План развертывания

### Вариант 1: Lite Mode (рекомендую для начала)
**Преимущества:**
- ✅ Быстрая установка (5 минут)
- ✅ Минимальные зависимости (SQLite + sentence-transformers)
- ✅ Можно сразу тестировать и дорабатывать
- ✅ Легко мигрировать на PostgreSQL потом

### Вариант 2: Production Mode с PostgreSQL
**Преимущества:**
- ✅ Полноценная БД
- ✅ pgvector для векторного поиска
- ✅ Масштабируемость
- ✅ Но требует больше настройки

## 🎯 Рекомендация: Начать с Lite Mode

### Шаг 1: Обновление кода на сервере

```bash
# Подключиться к серверу
ssh user@your-server

# Перейти в каталог проекта (или клонировать)
cd /path/to/docagent
# ИЛИ
git clone https://github.com/leval907/docagent.git /opt/docagent
cd /opt/docagent

# Обновить код
git pull origin main
```

### Шаг 2: Быстрая установка зависимостей

```bash
# Активировать существующее окружение или создать новое
source venv/bin/activate  # если есть
# ИЛИ
python3.11 -m venv venv
source venv/bin/activate

# Установить только необходимое для Lite
pip install sentence-transformers crawl4ai numpy

# Playwright браузер
playwright install chromium
playwright install-deps
```

### Шаг 3: Настройка окружения

```bash
# Создать .env файл
cp .env.example .env
nano .env

# Минимальная конфигурация для Lite:
SQLITE_PATH=/opt/docagent/data/sqlite/docagent_lite.db
KNOWLEDGE_BASE_DIR=/opt/docagent/knowledge_base
LOGS_DIR=/opt/docagent/logs
EMBEDDING_MODEL=all-MiniLM-L6-v2
```

### Шаг 4: Создание структуры каталогов

```bash
mkdir -p data/sqlite
mkdir -p logs
mkdir -p backups
```

### Шаг 5: Тестирование

```bash
# Загрузить существующие DuckDB документы
python scripts/pipeline_lite.py load knowledge_base/duckdb/docs-stable-index.md --app duckdb

# Проверить статистику
python scripts/pipeline_lite.py stats

# Тестовый поиск
python scripts/pipeline_lite.py search "SQL query" --app duckdb
```

### Шаг 6: Массовая загрузка

```bash
# Загрузить все DuckDB документы (займет 5-10 минут)
for file in knowledge_base/duckdb/*.md; do
    echo "Processing: $file"
    python scripts/pipeline_lite.py load "$file" --app duckdb
done

# Проверить результат
python scripts/pipeline_lite.py stats
```

## 🔧 Доработка и кастомизация

### Где находятся ключевые файлы:

```bash
scripts/pipeline_lite.py       # Основной pipeline - можно дорабатывать
scripts/crawler_crawl4ai.py    # Краулер - добавлять источники
config/sources.yaml            # Конфигурация источников
.env                          # Настройки окружения
```

### Примеры доработок:

**1. Изменить размер chunks:**
```python
# В pipeline_lite.py, строка ~25
def __init__(
    self,
    chunk_size: int = 800,      # Было 500
    chunk_overlap: int = 100    # Было 50
):
```

**2. Использовать другую модель embeddings:**
```bash
# В .env
EMBEDDING_MODEL=all-mpnet-base-v2  # Более точная, но тяжелее
```

**3. Добавить новый источник документации:**
```yaml
# В config/sources.yaml
your_app:
  name: "Your App"
  url: "https://docs.yourapp.com"
  depth: 2
  max_pages: 50
  enabled: true
```

## 🔄 Автоматизация (позже)

После того как все протестируете и настроите:

```bash
# Создать cron задачу
crontab -e

# Добавить строку (ежедневно в 2:00)
0 2 * * * cd /opt/docagent && venv/bin/python scripts/crawler_crawl4ai.py --app duckdb >> logs/crawl.log 2>&1
```

## 📊 Мониторинг

```bash
# Проверить размер БД
ls -lh data/sqlite/docagent_lite.db

# Просмотр логов
tail -f logs/crawl.log
tail -f logs/pipeline.log

# Статистика
python scripts/pipeline_lite.py stats
```

## 🚀 Миграция на PostgreSQL (когда потребуется)

Когда SQLite станет недостаточно:

```bash
# 1. Установить PostgreSQL зависимости
pip install psycopg2-binary

# 2. Создать БД PostgreSQL
sudo -u postgres psql -c "CREATE DATABASE docagent;"
sudo -u postgres psql -d docagent -f init-db.sql

# 3. Переключиться на PostgreSQL pipeline
# (пока используйте crawl_and_clean.py вместо pipeline_lite.py)
```

## 💡 Рекомендуемый workflow

```
1. Разверните Lite Mode (30 минут)
   ↓
2. Протестируйте на существующих данных (15 минут)
   ↓
3. Настройте под свои нужды (дорабатывайте)
   ↓
4. Когда все работает - настройте автоматизацию
   ↓
5. При необходимости - мигрируйте на PostgreSQL
```

## ❓ Быстрые ответы

**Q: Сколько памяти нужно?**  
A: 2-4 GB RAM для Lite режима

**Q: Можно ли использовать существующий PostgreSQL?**  
A: Да! Просто создайте БД `docagent` и запустите `init-db.sql`

**Q: Как добавить свою документацию?**  
A: Добавьте в `config/sources.yaml` и запустите краулер

**Q: Где хранятся embeddings?**  
A: В SQLite в таблице `chunks`, колонка `embedding` (JSON)

**Q: Можно ли использовать OpenAI API вместо локальных embeddings?**  
A: Да, замените `model.encode()` в `pipeline_lite.py` на `openai.Embedding.create()`

---

**✅ Готово к развертыванию! Время установки: ~30 минут**

Следующий шаг: `git pull && source venv/bin/activate`
